apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "locust-load-test.fullname" . }}-files
  labels:
    {{- include "locust-load-test.labels" . | nindent 4 }}
data:
  locustfile.py: |
{{ .Files.Get "files/locustfile.py" | indent 4 }}
